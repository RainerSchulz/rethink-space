# gitleaks-Konfiguration für den CI-Secret-Gate.
# Standard-Regeln ohne Ausnahmen: Diese Website hat keine öffentlichen Keys.
# Findet der Scan ein Secret, läuft weder Build noch Deploy.

title = "RE-THINK SPACE — gitleaks"

[extend]
useDefault = true
